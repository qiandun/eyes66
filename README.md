# eye-screen

visual studio code 护眼主题

- 提示样式全
- 样式展示柔和
- 关键字突出

## 打包

> 手动打包为 `.vsix` 文件，到[管理后台](https://marketplace.visualstudio.com/)上传
>
> 注意修改版本 -> package.json -> version

1. `npm install -g @vscode/vsce`
2. 打包：`vsce package`
3. 到后台选择 update 上传即可
